#include "location_container.h"

namespace rows {

    bool LocationContainer::Add(const Location &value) {
        return false;
    }

    int64 LocationContainer::Distance(const Location &from, const Location &to) const {
        return 0;
    }
}
